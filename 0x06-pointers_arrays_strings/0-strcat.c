#include "main.h"

/**
 * _strcat - concatnates
 * @dest: final
 * @src: starto
 * Return: always 0
 */
char *_strcat(char *dest, char *src)
{
	int i, k;

	for (i = 0; dest[i] != '\0'; i++)
	{
	}
	for (k = 0; (dest[i+n] = *src++) != '\0'; k++)
	{
	}
	return (dest);
}

