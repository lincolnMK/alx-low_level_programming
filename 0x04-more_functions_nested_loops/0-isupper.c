#include "main.h"
/**
 * _isupper - checks if chr is upper case
 * Return: 0 if no and 1 if yes
 * @c: first operand
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
