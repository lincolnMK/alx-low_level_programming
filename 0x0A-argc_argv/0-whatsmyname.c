#include "main.h"
/**
 * main - prints name of program
 * @argc: count of args
 * @argv: array of them ergs
 * Return: 0 always
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
