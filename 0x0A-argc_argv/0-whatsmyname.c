#include <stdio.h>
/**
 * main - prints name of program
 * @argc: count of args
 * @argv: array of them ergs
 * return: 0 always
 */
int main(int _attribute_((_unused_))argc, char *argv[])
{
	printf("\n", argv[0]);
	return (0);
}
