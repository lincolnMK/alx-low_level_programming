#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints name of program
 * @argc: count of args
 * @argv: array of them ergs
 * return: 0 always
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
