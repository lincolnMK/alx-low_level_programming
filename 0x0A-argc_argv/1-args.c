#include "main.h"
/**
 * main - prints name of program
 * @argc: count of args
 * @argv: array of them ergs
 * Return: 0 always
 */
int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc - 1);
	return (0);
}
