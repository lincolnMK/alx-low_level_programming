#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints name of program
 * @argc: count of args
 * @argv: array of them ergs
 * return: 0 always
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
